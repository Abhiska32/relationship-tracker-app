
  # Relationship tracker app

  A mobile app for couples to celebrate their relationship — day counter, birthday countdown, music player, and memory storage.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
